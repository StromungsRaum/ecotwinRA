# Purpose: Secret template for the ecotwin-operator to authenticate against the SIMOD API.
apiVersion: v1
kind: Secret
metadata:
  name: simod-api-credentials
  namespace: ecotwin-system
type: Opaque
stringData:
  SIMOD_API_TOKEN: "REPLACE_ME"
