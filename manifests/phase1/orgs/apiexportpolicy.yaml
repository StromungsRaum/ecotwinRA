apiVersion: core.platform-mesh.io/v1alpha1
kind: APIExportPolicy
metadata:
  name: orchestrate.platform-mesh.io
spec:
  apiExportRef:
    name: orchestrate.platform-mesh.io
    clusterPath: root:providers:ianus-provider

  allowPathExpressions:
    - :root:orgs:*
