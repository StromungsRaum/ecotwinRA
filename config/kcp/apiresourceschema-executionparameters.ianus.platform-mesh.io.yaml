apiVersion: apis.kcp.io/v1alpha1
kind: APIResourceSchema
metadata:
  name: v260907-e9c64e5.executionparameters.ianus.platform-mesh.io
spec:
  group: ianus.platform-mesh.io
  names:
    kind: ExecutionParameter
    listKind: ExecutionParameterList
    plural: executionparameters
    singular: executionparameter
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      description: ExecutionParameter is the Schema for the executionparameters API
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          description: ExecutionParameterSpec defines the desired state of ExecutionParameter
          properties:
            formId:
              description: |-
                FormID is the uuid of the SIMOD backend Form this execution parameter
                set is validated against (see `GET /api/platform/forms`).
              type: string
            parameters:
              additionalProperties:
                type: string
              description: Parameters are the form's input parameter values.
              type: object
            secretRefs:
              description: SecretRefs is an array of references to Secrets containing
                SIMOD credentials
              items:
                description: |-
                  SecretReference references a Secret by name in the same namespace as the
                  resource that holds the reference.
                properties:
                  name:
                    description: Name of the referenced Secret
                    type: string
                required:
                - name
                type: object
              type: array
          required:
          - formId
          - parameters
          type: object
        status:
          description: ExecutionParameterStatus defines the observed state of ExecutionParameter
          properties:
            message:
              description: Message carries error details when Phase is Failed.
              type: string
            phase:
              description: 'Phase is the current reconciliation phase: Pending, Ready,
                or Failed.'
              type: string
            token:
              description: |-
                Token is the opaque, encrypted execution-parameter reference the SIMOD
                backend issued. It is not persisted backend-side (there is no GET for
                this resource) and is redeemable for a Simulation - handle it like a
                credential, not a plain id.
              type: string
          type: object
      type: object
    served: true
    storage: true
    subresources:
      status: {}
